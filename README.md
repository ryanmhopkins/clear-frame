# ClearFrame

A local-first privacy metadata scanner and cleaner for JPEG, PNG, and WebP images.

## Privacy

Files are processed entirely in the browser and are never uploaded. ClearFrame removes ordinary privacy metadata while reporting provenance credentials separately.

## Run locally

Open `index.html` in a browser or serve this directory with any static file server.
